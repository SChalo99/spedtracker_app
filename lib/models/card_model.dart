class CardModel {
  String cardId;
  String cardNum;
  String type;
  String cardHolder;
  String expMonth;
  String expYear;
  String service;

  CardModel(this.cardId, this.cardNum, this.type, this.cardHolder,
      this.expMonth, this.expYear, this.service);
}
